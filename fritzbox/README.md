SpacePhone for Fritz!Box
========================

Status: **untested**

In the ``VOIPcfg`` section, enable ENUM support:

    NoChecks = yes;
    enum_support_enabled = yes;
    enumdomains = "e164.spacephone.org", "e164.arpa", "e164.org";
